server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/anujyadav11/event-platform-config.git
          default-label: main

management:
  endpoints:
    web:
      exposure:
        include: health,info