$kind: http-request
name: 'Get Inventory'
method: GET
url: '{{base_url}}/api/v1/inventory/:productId'
order: 1000
headers:
  - key: Authorization
    value: 'Bearer {{access_token}}'
pathVariables:
  - key: productId
    value: '{{product_id}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Inventory lookup is successful', function () {
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
