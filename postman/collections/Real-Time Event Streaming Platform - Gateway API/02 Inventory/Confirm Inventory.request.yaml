$kind: http-request
name: 'Confirm Inventory'
method: POST
url: '{{base_url}}/api/v1/inventory/confirm'
order: 4000
headers:
  - key: Content-Type
    value: application/json
  - key: Authorization
    value: 'Bearer {{access_token}}'
body:
  type: json
  content: |-
    {
      "productId": {{product_id}},
      "quantity": {{quantity}}
    }
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Inventory confirm is successful', function () {
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
