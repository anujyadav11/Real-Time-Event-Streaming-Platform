apiVersion: v1
kind: Service
metadata:
  name: saga-orchestrator
  namespace: eventstream
  labels:
    app: saga-orchestrator

spec:
  type: ClusterIP

  selector:
    app: saga-orchestrator

  ports:
    - name: http
      port: 8095
      targetPort: 8095
      protocol: TCP