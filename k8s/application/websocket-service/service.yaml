apiVersion: v1
kind: Service

metadata:
  name: websocket-service
  namespace: eventstream
  labels:
    app: websocket-service

spec:
  type: ClusterIP

  selector:
    app: websocket-service

  ports:
    - name: http
      port: 8088
      targetPort: http
      protocol: TCP