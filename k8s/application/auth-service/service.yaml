apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: eventstream
  labels:
    app: auth-service

spec:
  type: ClusterIP

  selector:
    app: auth-service

  ports:
    - name: http
      port: 8087
      targetPort: 8087
      protocol: TCP