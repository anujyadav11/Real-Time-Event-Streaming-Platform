apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  namespace: eventstream
spec:
  type: ClusterIP

  selector:
    app: delivery-service

  ports:
    - name: http
      port: 8085
      targetPort: 8085
      protocol: TCP