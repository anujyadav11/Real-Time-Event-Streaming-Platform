apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: event-platform

nameSuffix: -inventory-service

resources:
  - ../../common/deployment
  - ../../common/service
  - configmap.yaml
  - hpa.yaml
  - pdb.yaml

patches:
  - path: deployment-patch.yaml
  - path: service-patch.yaml
