apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

nameSuffix: -discovery-server

resources:
  - ../../common/deployment
  - ../../common/service

patches:
  - path: deployment-patch.yaml
  - path: service-patch.yaml

namespace: event-platform