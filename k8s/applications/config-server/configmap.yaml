apiVersion: v1
kind: ConfigMap

metadata:
  name: config-server-config
  namespace: event-platform

data:
  CONFIG_REPO_URI: "https://github.com/anujyadav11/event-platform-config.git"
  CONFIG_REPO_LABEL: "main"