apiVersion: v1
kind: Service
metadata:
  name: kafka-ui
  namespace: eventstream-infra

spec:
  type: ClusterIP

  selector:
    app: kafka-ui

  ports:
    - name: http
      port: 8080
      targetPort: 8080